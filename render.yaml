services:
  - type: web
    name: correctover-mcp
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install --production
    startCommand: node smithery-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8080
    healthCheckPath: /health
